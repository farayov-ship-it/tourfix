/** Admin UI barrel — Prisma YO‘Q (client komponentlar ham import qilishi mumkin).
 *  getEnabledLocaleCodes → `@/lib/admin/locales`
 */
export { default as LocaleFields } from "./LocaleFields";
export { default as ImagePicker } from "./ImagePicker";

export {
  parseMap,
  STATUS_OPTIONS,
  CATEGORY_OPTIONS,
  LOCALE_LABELS,
  statusLabel,
  categoryLabel,
  slugify,
  btnPrimary,
  btnDanger,
  btnGhost,
  fieldClass,
  labelClass,
  hintClass,
  Field,
  FormCard,
} from "./ui-styles";
